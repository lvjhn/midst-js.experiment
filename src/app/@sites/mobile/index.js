import CustomRoutes from './routes'
import AutoRoutes from './routes/_auto'

export default {
    /** ===== ROUTES ===== */
    routes: {
        auto    : AutoRoutes, 
        custom  : CustomRoutes
    }
}